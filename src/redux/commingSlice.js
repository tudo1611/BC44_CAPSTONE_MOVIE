import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const commingSlice = createSlice({
  name: "commingSlice",
  initialState,
  reducers: {},
});

export const {} = commingSlice.actions;

export default commingSlice.reducer;
